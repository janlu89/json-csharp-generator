export const environment = {
  production: true,
  apiUrl: '' // empty string means same origin — API and client served from same domain
              // on Render.com the API has its own URL so we'll update this on Day 11
};